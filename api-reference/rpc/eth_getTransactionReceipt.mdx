---
title: "eth_getTransactionReceipt"
description: "Returns the receipt of a transaction by transaction hash."
---

<Warning>
**Coming Soon** — The JSON-RPC proxy is not yet live. PolyNode's core settlement streaming API is fully operational. RPC access will be enabled once our Polygon node sync completes.
</Warning>

## eth_getTransactionReceipt

Returns the receipt of a transaction by transaction hash. Receipts are only available for confirmed transactions.

**Cache**: 24 hours (immutable once confirmed)

### Parameters

| Position | Type | Description |
|----------|------|-------------|
| 0 | `DATA` | 32-byte transaction hash |

### Returns

`Object` — transaction receipt, or `null` if not found.

### Example

```bash cURL
curl -s -X POST https://api.polynode.dev/rpc \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "eth_getTransactionReceipt",
    "params": ["0xabc123..."],
    "id": 1
  }'
```

### Response

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "transactionHash": "0xabc123...",
    "blockNumber": "0x5125d84",
    "status": "0x1",
    "gasUsed": "0x1a2b3",
    "logs": []
  }
}
```
